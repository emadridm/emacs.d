;;; init-grammar.el --- The Grammar Package Configuration -*- lexical-binding: t -*-

;; Author: Enrique Madrid

;;; Commentary:

;; The minimal grammar package configuration

;;; Code:

;; (require 'init-harper) ;; Disable because it does not work

;; (require 'init-ltex) ;; Disable because it does not work.

(provide 'init-grammar)

;;; init-grammar.el ends here
